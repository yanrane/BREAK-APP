{
  "version": 2,
  "installCommand": "corepack enable pnpm && cd ../.. && pnpm install --frozen-lockfile",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.ts" }
  ],
  "crons": [
    {
      "path": "/api/v1/cron/daily-missions",
      "schedule": "0 17 * * *"
    }
  ]
}
